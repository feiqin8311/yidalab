'use client';

import { memo } from 'react';

import SkillStoreHome from '@/features/SkillStore/Home';

/** Community market home — capability map above 助理 / Skills / MCP. */
const CommunityHomePage = memo(() => <SkillStoreHome />);

CommunityHomePage.displayName = 'CommunityHomePage';

export default CommunityHomePage;
